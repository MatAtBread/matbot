{
  "name": "@matatbread/matbot-mcp-http",
  "matbotRuntime": ["node", "browser"],
  "version": "0.1.0",
  "description": "Cross-platform MCP client: connect to remote MCP servers over HTTP/SSE and register their tools. Runs in the browser and Node (fetch only, no child processes).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@matatbread/matbot-plugin-api": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
