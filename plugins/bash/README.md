# @matatbread/matbot-tool-bash

This is a [matbot](https://github.com/MatAtBread/matbot) plugin.

Run bash scripts on the host, in a private scratch directory. For a container-isolated `bash` of the
same name and shape, load `@matatbread/matbot-tool-docker-bash` instead.
