---
"@matatbread/matbot-cli": patch
---

cli: the boot banner warns about a duplicated core/plugin-api copy by comparing resolved package directories rather than version numbers, which differ legitimately between independently versioned packages.
