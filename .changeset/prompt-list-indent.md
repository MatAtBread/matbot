---
'@matatbread/matbot-frontend-web': patch
---

Indent bulleted lists in a confirm prompt's question. A prompt that lists items — the plugin installer's
approval names one package per line — rendered them flush against the text with no visible bullet gutter.
